<footer class="footer">
  <div class="row align-items-center justify-content-xl-between">
    <div class="col-xl-6">
      <div class="copyright text-center text-xl-left text-muted">
        &copy; 2021
        <span class="font-weight-bold ml-1" target="_blank">Zekraoui Nasser & Zemmouri Rediane</span> 
      </div>
    </div>
    <div class="col-xl-6">
      <span>CDN Management Application</span>
    </div>
  </div>
</footer>

<script>
  function addEvent(event, elem, func) {
    if (elem.addEventListener) // W3C DOM
      elem.addEventListener(event, func, false);
    else if (elem.attachEvent) { // IE DOM
      elem.attachEvent("on" + event, func);
    } else { // No much to do
      elem["on" + event] = func;
    }
  }

  function redirectToLogin() {
    if (this.status === 200) {
      return window.location = this.responseURL || '/login';
    }
  }

  function logout(event) {
    if (!event.target.className.includes('logout')) {
      return;
    }

    var xhr = new XMLHttpRequest();

    xhr.addEventListener("load", redirectToLogin);
    xhr.open("POST", "/logout");
    xhr.send();
  }

  var bodyElement = document.querySelector('body');
  addEvent('click', bodyElement, logout);
</script>
